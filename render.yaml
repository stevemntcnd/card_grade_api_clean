services:
  - type: web
    name: card-grader-api
    env: python
    buildCommand: "pip3 install -r requirements.txt"
    startCommand: "python3 -m uvicorn server:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: OPENAI_API_KEY
        sync: false